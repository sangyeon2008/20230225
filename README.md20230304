# 20230225
# find 만들기
a="hello world"
def myfind(string,substr,start=0,end=-1):
    if substr in string:
        for i in range(start,end):
            if string[i]==substr[0]:
                return i
            else:
                return -1
                

    # if end == -1:
    #     end = len(string)
    # for i in range(start,end):
    #         if string[i] == substr[0]:
    #             return i
print(myfind(a,"wor",0,-1))
